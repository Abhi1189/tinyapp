<!doctype html>
<html>
    <head>
        <title>TinyApp!</title>
    </head>

    <body>
        <header>
            <% include ./partials/_header %>
        </header>
        <ul>
        <% for(let key in urls) { %>
            <li><%= key %> - <%= urls[key] %></li>
        <% } %>
        </ul>
        <a href="/urls/new">Shorten the URL here..</a>
    </body>
</html>